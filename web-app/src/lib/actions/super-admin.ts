/**
 * TEMPORARY STUB - Super admin actions for compatibility
 * Single-tenant: these are no longer used
 */

'use server';

export async function getSuperAdmins() {
  return { data: [], error: null };
}
